# Prompt History Note — Module 07

No contemporaneous prompt-history *file* exists for this module.
Structured, entry-by-entry prompt-history logging is a practice that
started later in the course; modules 11 onward carry a real, live
`prompt-history.md` instead of this note.

## L07 — TensorFlow Playground

Reconstructed shape of the work: running the 5 experiment tasks and
recording real epoch/loss numbers is hands-on work that had to be done
directly in the tool — not something an AI assistant could do on my
behalf. AI assistance was more likely used to help structure the report
format (per the Module 00 technical-report template referenced in the
assignment) and to source the citations backing the "why" behind each
result (Nair & Hinton on ReLU, Minsky & Papert on XOR/perceptron limits).

## A07 — Predicting HVAC Failures with a Neural Network

This is the one backfilled module where the original deck itself documents
that an AI-assisted build pipeline was used (credited on the References
slide: "Deck built with an AI-assisted pipeline... per course AI policy").
That credit line existed at submission time; a structured, entry-by-entry
prompt-history log did not.

**Reconstructed shape of the work (not a transcript):** The domain
knowledge (which four features matter, why, the RTU-04 scenario's
deliberate age-vs-recency tension) is mine, drawn directly from real HVAC
field experience. AI assistance was used to build the visual deck itself
and to source/verify the deep-learning citations (Goodfellow et al.,
Rumelhart et al., LeCun et al., Nair & Hinton, Glorot et al.) backing each
theory callout, and to keep the worked forward-pass/backprop numbers
internally consistent across slides.

Going forward, this note is replaced by a real prompt-history log kept while
the work is happening.
