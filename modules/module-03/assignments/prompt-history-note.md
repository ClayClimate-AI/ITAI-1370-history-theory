# Prompt History Note — Module 03

No contemporaneous prompt log exists for this module. Structured
prompt-history logging is a practice I started partway through the course;
modules 11 onward carry a real, live `prompt-history.md` instead of this
note.

## L03 — Scratch Paddle Game

Reconstructed shape of the work: development began by watching the
professor's Scratch tutorial to learn the block/sprite paradigm — that's
documented directly in the submission itself. From there, the conditional
logic, loops, and the score-reset debugging were hands-on work done
directly in the Scratch editor, not something delegable to an AI
assistant (Scratch's block interface isn't something a text-based AI tool
edits directly).

## InClass Assignment — Stock Sentiment Tracker

Reconstructed shape of the work: building a working RSS-crawling,
client-side sentiment scorer with CORS handling and a fallback mode is
real engineering work that almost certainly involved substantial AI
assistance for the fetch/proxy/scoring logic. The scoring methodology
(weighted keywords, negation detection, intensity modifiers) and the
choice of five tracked tickers are decisions that required my direction —
this wasn't a one-shot "build me a sentiment tracker" prompt.

## A03 + Puzzle 03

**Reconstructed shape of the work (not a transcript):** P03's references
list credits an interactive Monty Hall simulator built with AI assistance
specifically to test the probability claim empirically (play multiple
rounds, watch the win rate) rather than accept the Bayes' Theorem proof on
faith. For A03, AI assistance was used to source and verify the academic
citations (Arulkumaran et al., Justesen et al., Vinyals et al.) backing each
claimed AlphaStar limitation — the four-limitation structure and the writing
are mine.

Going forward, this note is replaced by a real prompt-history log kept while
the work is happening.
